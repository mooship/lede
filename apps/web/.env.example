# Web environment variables — copy to .env.development or .env.production and fill in values
# .env.development is committed; .env.production is gitignored

# Base URL of the API worker
VITE_API_URL=http://localhost:8787

# Public URL of this web app — used for og:url and canonical link tags
VITE_APP_URL=http://localhost:5173
